{
  "name": "senior-quote",
  "version": "1.0.0",
  "description": "A Caesar cipher puzzle that leads to my senior quote",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "gh-pages": "^6.3.0"
  }
}
